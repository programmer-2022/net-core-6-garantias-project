1. Montar contenedor de PostgreSQL y pgadmin

```cs
  > docker-compose -f ./docker-compose-local.yml up -d
```

2. Compilar proyecto

```cs
  > dotnet build
```

3. Ejecutar proyecto

```cs
  > dotnet run
```
